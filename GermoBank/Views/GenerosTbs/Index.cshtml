@model IEnumerable<GermoBank.Models.GenerosTb>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreGenero)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdFamiliaFkNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreGenero)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdFamiliaFkNavigation.IdFamiliaPk)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdGeneroPk">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdGeneroPk">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdGeneroPk">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
